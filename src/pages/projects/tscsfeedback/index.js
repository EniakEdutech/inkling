import {NavMenu} from '../../../molecules/navmenu'
import { Title} from '../../../atoms/title'

export const TCSCFeedback = props => {
    return <div>
        <NavMenu />
        <Title>TCSC Feedback</Title>
    </div>
}